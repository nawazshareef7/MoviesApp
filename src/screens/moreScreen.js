import React, { useEffect, useState } from 'react';
import { Text, FlatList,Image, View,Button, TouchableOpacity, Alert,ActivityIndicator } from 'react-native';

 export default class MoreScreen extends React.Component{
    render(){
      return (
        <View style={{ flex: 1 }}>
         
          
      </View>
      )
    }
  }