import React, {Component, useEffect, useState } from 'react';
import { Text, FlatList,Image,Button, View, TouchableOpacity, Alert,ActivityIndicator } from 'react-native';

export default class searchScreen extends React.Component{
    render(){
      return (
        <View style={{ flex: 1 }}>
       
      </View>
      )
    }
  }